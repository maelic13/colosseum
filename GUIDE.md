# Colosseum — Progress Guide

Quick, glanceable progress tracker. Each step is one commit. See [PLAN.md](PLAN.md) for full
detail. Model labels: **Small** = Sonnet 4.6 / GPT-5.5 medium · **Large** = Opus 4.8 high
thinking / GPT-5.5 extra high.

## Phase A — Foundation
- [x] **Step 0** — Initialize repository (git, `.gitignore`, GPL-3.0 `LICENSE`) · _Small_
- [x] **Step 1** — Author `PLAN.md` + `GUIDE.md` · _Small_
- [x] **Step 2** — Architecture scaffold (workspace, crates, deps, public seams, CI stub) · _Large_

## Phase B — Backend (logic, no UI)
- [x] **Step 3** — `colosseum-core`: domain types, Elo, pairings, adjudication, standings + unit tests · _Large_
- [x] **Step 4** — `colosseum-uci`: UCI protocol & process management + temp-Stockfish tests · _Large_
- [x] **Step 5** — `colosseum-engine`: scheduler, Go/Stop/Force-Stop, PGN, persistence + integration tests · _Large_
- [x] **Step 6** — Persistence & resume wiring; `--portable` mode · _Small_

## Phase C — GUI
- [x] **Step 7** — GUI scaffold, modern theme + app icon, backend bridge, close-confirm · _Large/Small_
- [x] **Step 8** — Engine Management tab (add, add-folder, auto-detect & edit options, metadata) · _Small_
- [x] **Step 9** — Tournament tab: options, Go/Stop/Force-Stop, **live sortable results table** · _Large_
- [x] **Step 10** — Starting positions / openings (EPD + PGN, integrated UI) — final feature · _Large_

## Phase D — Release
- [x] **Step 11** — Cross-platform packaging & release (cargo-dist; Flatpak/MSI/DMG/tarball) · _Small_
- [x] **Step 12** — README, CHANGELOG, docs polish · _Small_

---

### v0.1.0 shipped ✓

All 12 steps complete. See [README.md](README.md) for build & run instructions,
[CHANGELOG.md](CHANGELOG.md) for what's in v0.1.0, and [PLAN.md](PLAN.md) for the
full architecture reference.

---

## Phase E — Post-v1 enhancements (v0.2+)

Ordered by value/effort; see [PLAN.md §11](PLAN.md) for detail.

- [x] **Step 13** — Engine identity: detect `author` into `meta.extra`, parse version from `id name`, show Author field · _Small_
- [x] **Step 14** — Apply/sync resulting Elo from a tournament back to the engine library · _Small_
- [x] **Step 15** — Per-game timing → live **avg game time / elapsed / throughput / ETA**; set `games.started_at` · _Small_
- [x] **Step 16** — Wire **Resume** into the GUI (`resume_tournament` already exists) · _Small_
- [x] **Step 17** — **Tournament History** tab (`list`/`load`/`delete`/`resume`) · _Large_
- [x] **Step 18** — Live **"currently playing"** panel (in-flight games) · _Small_
- [x] **Step 19** — **Termination breakdown** summary in live view · _Small_
- [x] **Step 20** — Engines-tab usability: broken-path indicator, clone, search/filter+sort, per-option reset, open-folder, Button-option handling · _Small_
- [x] **Step 21** — Time controls: increment / sudden-death / nodes / depth · _Large_
- [x] **Step 22** — Gauntlet format + honest Format control (knockout / SPRT deferred — need a dynamic scheduler) · _Large_
- [x] **Step 23** — Config presets + remember last-used config · _Small_
- [x] **UI hardening** — systemic fixes for poorly-visible controls and layout jitter (see [PLAN.md §11](PLAN.md)) · _Small_
- [x] **Step 24** — Output & analysis: CSV export, export-PGN-now, SPRT/LOS/error bars, PGN/board viewer · _Large_
  - [x] **24a** — CSV standings/crosstable export + export-PGN-now (Export ▾ menu in live + history)
  - [x] **24b** — SPRT / LOS / Elo error bars (match-stats card for 2-engine tournaments)
  - [x] **24c** — Built-in PGN/board viewer (History → Games → View; live view → Games toggle)
- [x] **Step 25** — Cleanup: remove the unused SQLite `engines` table + dead `Store` engine methods · _Small_
- [x] **Step 26** — Engines-tab visual refresh: monogram avatars, version chips, author-as-subtitle, redesigned edit header (en-croissant-inspired, UCI-sourced identity) · _Small_
- [x] **Step 27** — Board live view fix (responsive wrapped control bar + finished-only Games list), shared engine-row widgets in tournament setup, pill-tab hover, overflow/wrap fixes, resize/fullscreen hardening · _Small_
- [x] **Step 28** — Engines tab restructure: 50% card grid + split right panel, engine logos (pick/copy/aspect-fit render), scrollable filtered UCI options (hide threads/hash/TB paths), global Syzygy/Gaviota/Nalimov tablebase paths applied at tournament start · _Large_
- [x] **Step 29** — Engines tab polish: fixed-size aligned cards (sorted A–Z), autosave with debounce (Save button removed), pinned Clone/Delete row, card context menu (clone / re-detect / open folder / copy path / delete-with-modal), high-quality logo rendering (per-size Lanczos textures), half-width identity fields + larger logo, smarter name/version detection (SIMD-noise stripping, multi-token versions like "14 WCSC") · _Medium_
- [x] **Step 30** — Engines tab en-croissant card redesign (GUIDELINES §3.9): spacious 98-pt cards targeting ~360 pt width (1–3 columns), name + author subtitle on top, labeled ELO / VERSION stat columns at the bottom, no chips inside cards; repaint-while-detecting fix (background detect results no longer wait for the next input event) · _Small_
- [x] **Step 31** — Engines tab polish round 2: solid non-overlapping scrollbars (global), rounded-corner logo rendering (textured `RectShape`), logo centered in header space with a stable Remove button, compact filter field with inline ✕ clear, multi-column UCI options on wide panels, collapsible Endgame Tablebases bar (collapsed by default with per-format ✓/— summary), body clipped via `CentralPanel` so the status bar survives small windows · _Small_
- [x] **Step 32** — Embedded fonts + Engines tab polish round 3: Inter (UI) / Inter-SemiBold (real bold via `theme::semibold`) / JetBrains Mono (numbers) embedded for identical cross-platform rendering, missing-glyph "tofu squares" eliminated (× clear button, ●/○ tablebase summary, painted disclosure triangle, plain "saved" flash), zero hover expansion (no more clipped focus rings / shifting buttons), whole detail column scrollable, rounded-square monogram avatars matching logo silhouette, aspect-adaptive header logo box (wide logos use the width), persisted grid sort (Name / Elo / Author in `AppConfig::engines_sort`), tablebases summary hidden when too narrow · _Medium_
- [x] **Step 33** — Micro-polish: sort popup sized to its three entries (no phantom scrollbar), context menu padding (`menu_margin` 10 + roomier item spacing), `selectable_labels = false` theme-wide so card/label text isn't text-selectable; verified UCI defaults display against a live engine dump (Shredder's odd values — Ponder=true, UCI_Elo=1400 — are genuinely what the engine declares) · _Small_
- [x] **Step 34** — Tablebase probe caches + startup polish: global Nalimov/Gaviota cache sizes (`NalimovCache` / `GaviotaTbCache`, default 32 MB, on `AppConfig`, edited inline in the tablebases bar, injected at tournament start when the matching path is set, hidden from per-engine editors), sort selector switched to a `MenuButton` popup (ComboBox popups force a ScrollArea that flashed a scrollbar), window starts hidden and is revealed after the first painted frame (no startup blink); design decisions consolidated as GUIDELINES §7 for the Tournament/History redesign · _Small_
- [x] **Step 35** — UCI options measured layout: column count derived from the widest actual row (label + editor + range hint measured via galley layout) instead of a fixed 420-pt estimate, so engines with long option names (Rybka's milipawn set) fall back to fewer columns and never collide; each column clipped as a hard guarantee; tablebases rows reordered Syzygy → Nalimov → Gaviota · _Small_
- [x] **Step 36** — Scroll/header fixes: options-column clip made horizontal-only (the vertical clip broke scrolling inside the detail ScrollArea — options were cut mid-row and unreachable at small windows), fixed-geometry engine header (identity fields and logo slot at identical position/size for every engine; logo image aspect-fitted and centered on a constant point, Remove row always reserved so nothing shifts), word-wise engine filter ("shredder 12" matches name+version terms independently), startup reveal delayed to the third pumped frame to kill the residual first-paint blink · _Small_
- [x] **Step 37** — Shape-language unification: header tabs and status pills are rounded rectangles at the app-wide radius 6 (no more pill radius 14/10), checkboxes are rounded squares radius 3 via new `widgets::checkbox` (egui's default radius 6 made ~16 px boxes read as circles — converted everywhere incl. Tournament tab), and one standard `widgets::clear_button` ("×" right of the field, appears when non-empty) shared by the tablebase rows and the engine filter · _Small_
- [x] **Step 38** — Scrollbar-free dropdowns app-wide: new `widgets::select` (MenuButton-based dropdown with painted arrow) replaces every `egui::ComboBox` — UCI combo options, tournament Format/Time-Control/Elo-policy/time-unit selectors, engines sort — because combo popups force a ScrollArea that shows a phantom scrollbar even for 3–5 items · _Small_
- [x] **Step 39** — Ponder hidden from the per-engine UCI editor: the scheduler always forwards the tournament's Ponder setting (overriding any per-engine value), so the per-engine checkbox was non-functional; Ponder joins Threads/Hash as "managed elsewhere" (set per tournament in Engine Options), tooltip updated to list all globally-managed options; plus 14 pt padding after the last UCI option row (before the pinned Clone/Delete divider) · _Tiny_
- [x] **Step 40** — Tournament/History/viewer tabs to GUIDELINES §7 standard: all tofu glyphs replaced (▶ ◀ ⏮ ⏭ ⏸ ⏹ ✓ ▲ ▼ ▾ ⟳ 🏛 → font-safe ↑ ↓ ‹ › « » ● or painted `dropdown_arrow`s on the Presets/Export menus), section/table/list headers to real semibold, Cycles range 1–10 000, Games/pair reduced to a 1-or-2 dropdown (clamped in `build_config` and preset loading — repetition is what Cycles is for), live schedule summary in the Tournament card ("This will play N games · estimated ~1h 20m", ~60 moves/side assumption, ÷ parallel games) with the same estimate in the Start bar, and time-control hints humanized via `format_clock` (shown only when the unit isn't ms; raw "= 120000 ms" gone) · _Medium_
- [x] **Step 43** — Tablebase globals + option-name mapping + compatibility notes: Endgame Tablebases widget gains Syzygy 50-move rule (default on) + probe limit (3–7, default 7) + Gaviota compression (cp0–cp4, default cp4) on `AppConfig`, injected with the matching path and hidden from per-engine editors; the tournament Syzygy50MoveRule checkbox removed; scheduler now forwards common Threads/Hash to whatever each engine calls them ("Max CPUs", "Cores", "Memory"…) clamped to the declared range (unit-tested — a literal "Threads" to Rybka was silently ignored before); override dialog fills its width (scrollbar at the right edge) and pre-fills library-effective + tournament-common values (Rybka's Max CPUs shows the tournament Threads); "⚠ N compatibility notes" badge in the Start bar lists engines that can't honour the settings (missing thread option → single-threaded, Hash capped/raised, missing exe) with overridden engines excluded; duplicate bottom status pill removed (header pill is the single source); logo loading budgeted to 3 decodes/resizes per frame — with ~25 logo-carrying engines the old first-paint hit froze the app for seconds, now logos fill in progressively · _Large_
- [x] **Step 55** — Native file dialogs open on the app's monitor: rfd "Browse…" pickers are real OS windows, and without a parent Windows placed them on the primary monitor — a different screen from Colosseum on multi-monitor setups. Now parented to the main window (so the OS centres them over it and makes them app-modal). The window/display handles are captured once at startup from `CreationContext` (`HasWindowHandle`/`HasDisplayHandle`) and stashed in a main-thread `thread_local`, so every call site — including free functions with no app state (`export_ui`, the `TournamentForm` section fns) — builds a parented dialog via `crate::dialog::file_dialog()` in place of `rfd::FileDialog::new()` (8 sites). In-app egui modals were already fine (drawn inside the window); the pre-window startup error `MessageDialog` is left unparented (no window exists yet) · _Small_
- [x] **Step 54** — Delete in-progress tournaments + reliable Windows taskbar icon: the live-view control bar gained a two-step **Delete** (Delete → Confirm delete) that works even while running — it force-stops the tournament, unloads it, and removes it from the database (previously Delete was only on stored/finished tournaments in the history detail). Taskbar icon fix: the app only set the window icon at runtime (`with_icon`), so on the hidden-window→reveal path Windows fell back to the exe's (absent) embedded icon and showed a blank/generic taskbar button. Added a `build.rs` that embeds a generated multi-size `assets/colosseum.ico` (matching the amphitheatre emblem) into the exe via `winresource` (non-fatal if no resource compiler is present), plus an explicit `SetCurrentProcessExplicitAppUserModelID` for a stable taskbar identity · _Small_
- [x] **Step 53** — Thread-option matcher: substring heuristic → explicit allowlist. Enumerating every thread/cpu/core option across the real 33-engine library showed the `contains("thread"/"cpu"/"core")` heuristic matched **13 distinct options, only 2 of them thread counts** — false-positives included numeric Spins that would be silently corrupted (`Score Offset millipawns` via "s**core**", `ThreadIdlingThreshold`), Lc0's `CPuct*`, and every option containing "Score". Step 52's blacklist (exclude "usage"/"percent") fixed CPU Usage but would still have clobbered `Score Offset millipawns` and `ThreadIdlingThreshold`. Replaced with a whitespace-/case-insensitive **allowlist** of real count-option names (`Threads`, `Max CPUs`, `CPUs`, `Cores`, `MaxThreads`, `Number of Threads`, `NumThreads`, `Core Threads`) — consistent with `is_hash_option`'s existing exact-match design; an unrecognised name is safely left at its default (visible miss vs silent corruption) and can be set per-engine or added to the list. Regression test `thread_option_rejects_the_real_library_false_positives` pins all 11 real false-positives · _Small_
- [x] **Step 52** — Fixed the "CPU Usage" throttle crippling Rybka 4/4.1/2.3.2a: the user's 15-parallel run (well under their 32 logical cores) still had Rybka 4/4.1 at ~10 k nps and losing 827/1400 by checkmate, while Rybka 3 was fine at 146 k. Ruled out CPU starvation empirically (15 concurrent single-threaded Rybka 4 = ~159 k nps each; isolation = 206 k), tablebases (identical middlegame depth with/without Nalimov), and time management (clock-based `go` gave depth 8). **Root cause:** `is_thread_option` matched *anything* containing "cpu", so it matched Rybka's **"CPU Usage"** option — a 1–100 % throttle, not a thread count — and `insert_matched` set it to the thread value (1), capping those engines at **1 % CPU** (confirmed by probe: CPU Usage=1 → depth 7 / 5.9 k nps vs CPU Usage=100 → depth 12 / 236 k nps, a 40× throttle). Rybka 3 lacks that option, hence unaffected. **Fix:** `is_thread_option` now excludes names containing "usage"/"percent"/"%", and `insert_matched` only writes to Spin (count) options so boolean toggles like HIARCS's "Busy Threads" aren't clobbered either. Regression-tested in core (`thread_option_excludes_cpu_usage_percentage`) and the scheduler (`thread_mapping_leaves_cpu_usage_and_bool_toggles_alone`). Diagnostic method reused: direct UCI probes of the engines from a shell + SQLite per-engine option/nps queries · _Medium_
- [x] **Step 51** — Setup-crash forensics + diagnosis of the Grand-Tour failures: investigated Junior's crashes and Rybka 2.3.2a's time losses using the incident logs + a direct UCI probe + the SQLite termination breakdown. **Root causes:** (1) Rybka 2.3.2a's 100 time-forfeits = severe CPU oversubscription — its incident transcripts show it running at ~40 k nps (vs its normal 1–2 M+, i.e. 2–3 % speed) and overshooting a 3.21 s budget by taking 5.11 s on one search, draining its clock; Colosseum's wall-clock accounting is correct and already lenient (2 s tolerance), so this is a resource problem, not a bug — the step-49 oversubscription warning is the right guard. (2) Junior's 17 crashes left **no incident files** because setup failures (spawn/handshake/`ucinewgame`) returned early via `setup_failure` before the incident writer, and the failed process (with its stderr/transcript) was dropped — Junior handshakes + searches perfectly in isolation, so it was dying on startup under load. **Fixes:** `prepare` now returns a `Prepared` enum retaining the process on failure; `handle_setup_failure` quits the survivor, harvests the failed engine's UCI transcript + stderr, and writes a `SetupCrash-*` incident (integration-tested with a `cmd /c exit` stub); the handshake/isready timeout was raised 10 s → 30 s so a *slow* startup under heavy parallelism is no longer misreported as a crash. Also confirmed the scheduler can run one engine in several concurrent games, so Junior's `Book_learning=true` shared-file writes are an additional crash vector under high concurrency (documented; mitigation = fewer parallel games or disable learning) · _Medium_
- [x] **Step 50** — Instant tournament start: the schedule was persisted one `INSERT` per game — one implicit transaction and disk sync each, so a 10 500-game tournament froze the UI for minutes at Start. `Store::insert_pending_games` now writes the whole schedule in **one transaction** (measured on disk: 10 500 games in ~90 ms, timing-asserted by an integration test and logged at start); the resume path's per-row reset loop became a single `reset_unfinished_games` UPDATE. No progress dialog needed — starting is effectively instant now · _Small_
- [x] **Step 49** — Duplicate-engine dialogs, logging system, and game-failure forensics: adding an engine that's already in the library (same executable path *or* same detected name+version, case-insensitive) now raises a confirm dialog ("Add anyway" / "Cancel"); a folder scan collects all duplicates into one checkbox dialog (nothing ticked by default, Select all / Deselect all, "OK (import N)" / "OK (skip all)") while non-duplicates import as before — verified live against `test_engines` (7 duplicates found incl. identity matches at different paths, all skipped). **Logging**: `data/logs/colosseum.log` (file + console, 4 MB rotation, session banner) and `data/logs/incidents/*.txt` — every crash / illegal move / time forfeit now writes a forensic report with engines, time control, clocks at game end, start FEN, full move list, the offending detail, each engine's last ~120 UCI protocol lines (info spam collapsed) and its captured **stderr** (previously `Stdio::null` — engines print their panic/assert messages there); the Results error panel links the file. **Root cause found & fixed**: the runner forwarded each engine's *raw* bestmove text into the shared move list — and shakmaty silently accepts Chess960-style castling (`e1h1`) in standard games, so a nonstandard-notating engine desynced its *opponent*, whose later (locally-legal) moves were then judged illegal — moves are now canonically re-encoded before entering the history (unit-tested). Bare promotions (`d7d8`, Rybka-style) and uppercase moves are now tolerated with a logged warning instead of an instant loss. New compatibility note warns when parallel games × threads exceed the machine's logical cores (wall-clock timed games + CPU starvation = time forfeits and crashy old engines) · _Large_
- [x] **Step 48** — Multiple simultaneous tournaments + Results-tab UX: `Backend.actives` is now a `Vec` — several tournaments can be loaded (and even running) at once, each with its own driver, snapshot, priors and writeback; **clicking an unfinished tournament in the Results list loads it instantly** (same state as Resume: stopped, ready to Go) so switching between test runs is one click; a newly started/resumed tournament steals the selection; the header pill aggregates (Running beats Stopping beats Stopped); quitting stops all of them. The list **auto-refreshes** (≤1 s staleness + after every action — Refresh button removed), rows show live progress ("109 / 1400 games") and consistently capitalized states derived from reality: **Live** = games in flight, **Finished** = all played, **Stopped** = anything loaded-but-idle or stored-unfinished (a stored "running" row has no driver, so it shows Stopped). The startup "Unfinished tournament found" banner is gone — the Results list is the one place for resuming. Live bar's "New Tournament" became **Close** (unload; results stay listed). Tab order Tournament · Results · Engines. Setup: shared `widgets::filter_field` (28 pt, same everywhere), Select all/Clear sized to the standard interact height so the row matches the Sort select; engine rows get explicit id salts (transient duplicate-ID flash). Verified live with the user's real gauntlet loaded read-only alongside a running test match, switching between both · _Large_
- [x] **Step 47** — Results tab (live + history merged) + ML ratings + live concurrency: new **Results** tab replaces History and hosts the live view — tournament list on the right (active pinned "● live"), main area shows the live tournament or any stored one (Resume/Delete/Export there; starting or resuming auto-switches to it; Tournament tab is setup-only and blocks Start while something is live). Live table: merged "Name version" engine column, per-engine **Forfeits** column (n× time / n× crash, tracked in `Standings` via `GameOutcome::termination`), proper margins, H2H+Games stack without dead space (the horizontal H2H ScrollArea claimed all remaining height — `auto_shrink` vertical). **Elo column always shows exactly what the configured writeback stores**: Never → static library ratings (Δ from the live model), All → joint **maximum-likelihood ratings** (`core::ml_ratings`, damped synchronous performance-rating iteration anchored to the participants' mean prior, unit-tested; K-factor UI removed), Estimate → target's performance rating with everyone else anchored (never moves, not even live); priors pinned at start so Δ survives the auto-writeback; the manual apply button is mode-aware ("Apply Fruit estimate → Library"). **Head-to-head per format**: gauntlet = seed engine on top with per-opponent score rows; round robin = crosstable; both with a W-D-L / per-game-results toggle ("1 0 ½" — ½ renders in Inter). **Parallel-games control in the live bar** (`Command::SetConcurrency`): running games always finish, only the launch rate changes, works stopped or running, persisted for resume, ETA uses it (measured avg per game once available, TC-based estimate before). Engine errors humanized ("Fruit 2.1 crashed vs Rybka 3 (round 2) — loss awarded. Detail: …"). Setup polish: all small buttons normal-framed (Select all/Clear/Browse…/×), tablebase note at the section bottom, Start Tournament bottom-right with summary+warnings beside it, resume banner hidden while live and reflows on dismiss. Verified with a live gauntlet incl. estimate auto-writeback (displayed 3229 == written 3229, opponents untouched; test data cleaned up) · _Huge_
- [x] **Step 46** — Setup estimates split + hover-shift eliminated + list sorting: the Time Control card shows the single-game estimate ("1 game: ~10s"), the Tournament card the whole schedule ("2 engines · 200 games · ~2m 06s", semibold values with dot separators); root cause of buttons shifting on hover found by pixel-diffing screen captures — egui's `selectable_label` has no frame while idle and gains frame padding when hovered, pushing everything to its right — replaced by always-framed `widgets::choice_chip` (rule: never use selectable_label/value in row layouts, popup menus only); tournament-setup engine list gets the same persisted Name/Elo/Author sort as the Engines tab (`AppConfig::tournament_engines_sort`, shared `widgets::EngineSort` + `sort_engine_indices` + `engine_sort_select`); Output's cut-off hint replaced by a short "optional" hint with the explanation on hover; Openings + Output audited as fully implemented (EPD/PGN parsing incl. FEN tags/comments/NAGs, deterministic seeded shuffle, count cap, one opening per encounter shared by both colours with modulo cycling, live preview, per-game PGN append) · _Small_
- [x] **Step 45** — Real startup fix + setup usability + Elo writeback redesign: the *actual* blink was the maximized restore — winit on Windows can only maximize a window by showing it, so `with_maximized(true)` + hidden start flashed an empty maximized frame for ~16 ms (caught with a visibility-transition monitor); maximizing now happens at reveal time, after the first painted frames, and the monitor confirms the window becomes visible exactly once. Engine rows get a proper bordered `widgets::dots_button` (painted dots, vertically centered, accent-tinted when overrides exist) instead of a baseline-aligned "…" glyph. Gauntlet's numeric "Seed engines" replaced by a "Gauntlet engine" dropdown of the currently selected engines (picking one moves it to the front of the seeding order). Every time-control type now has a one-line explanation. The Elo card is redesigned around *library-rating writeback*: "Update ratings" = Never (default) / All engines (K-factor shown) / Estimate one engine (engine picker defaulting to the gauntlet engine) — implemented as `TournamentConfig::rating_writeback` applied automatically on finish; Estimate computes a proper performance rating (`core::performance_rating`, bisection on the logistic model, ±800 caps, unit-tested) against the opponents' *fixed* library ratings, so gauntleting a new engine rates only that engine; the live standings Elo column always ticks per game regardless. Default tournament name is "Tournament" (and no longer restored from last-used settings). Verified end-to-end with a real 4-game gauntlet: Fruit (0/4 vs Rybka 3 @3078) was written back as 2278 = 3078 − 800 with Rybka untouched; test data cleaned up after · _Large_
- [x] **Step 44** — Setup polish + true instant startup: startup blink root-caused with a window-creation monitor — the debug binary was a *console-subsystem* exe, so Windows flashed a `PseudoConsoleWindow` at every launch (`windows_subsystem = "windows"` applied only in release); now unconditional, with `AttachConsole(ATTACH_PARENT_PROCESS)` restoring tracing output when launched from a terminal; header tabs no longer shift on selection (pill_tab always reserves the wider semibold galley width) and standings headers reserve the sort arrow via a transparent glyph; the "⚠ N compatibility notes" badge is a real tinted-warn button opening a modal that lists every note at once (hover kept); engine-selection rows gain a hover-revealed "…" button (fixed 20-pt slot so rows never shift) opening the Tournament-UCI-options editor directly, with the right-click menu kept as the alternate path — row hover/fill switched from `hovered()` to `contains_pointer()` because the child button taking hover ownership flickered the row and swallowed the reveal; settings cards get a 12-pt left margin so they no longer touch the engine-panel separator line · _Small_
- [x] **Step 42** — Per-engine per-tournament UCI overrides: `TournamentConfig.engine_overrides` (serde-default, persisted for resume), applied by the scheduler at the *highest* precedence (library options < tournament common < per-engine override — precedence unit-tested; the "Rybka 3 can't take the common Hash" case), edited via right-click on an engine-selection row → "Tournament UCI options…" modal (full detected option list with library-effective defaults, per-option × reset, Reset all, live "N options overridden" count) with an accent ● indicator on rows carrying overrides and a "Clear tournament overrides" menu item; schedule/estimate labels trimmed to bare "40 games" / "~4h 12m" (details on hover) · _Medium_
- [x] **Step 41** — Tournament setup rework: two-column card layout (whole form + Start visible without scrolling on a normal window), engine selection restyled (filter + filtered Select-all, compact rows with logos/monograms, semibold names, right-aligned Elo, whole-row toggle), duration estimate moved to the Time Control card ("Estimated length ~D (N games, L in parallel)") with games count staying in the Tournament card, all per-field ms/unit-conversion hints removed, last tofu glyphs (🗑 ⧉) dropped; verified end-to-end with a real 4-game 100 ms/move tournament (estimate ~50 s vs 58 s actual), then the test tournament was deleted from history · _Medium_

---

### Deferred (architecture-ready)
- [ ] Error-bar / Ordo rating recompute (see Step 24)
- [ ] Engine process pool
- [ ] Tablebase-based adjudication (optional feature, off by default)
- [ ] macOS notarization · UCI_Chess960 · localization
